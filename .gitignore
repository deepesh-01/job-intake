.env
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
data/
*.json
.pytest_cache/
.DS_Store

# Webapp build artifacts
webapp/node_modules/
webapp/dist/
webapp/.vite/

# Local launchd logs
data/web.*.log
data/launchd.*.log
*.bak
CLAUDE_resume_tailoring_runtime.md
webapp/tsconfig.tsbuildinfo

# Transient marker written by `make docs-sync` after each apply
docs/.docs-sync-last.txt

# BMAD-METHOD personal config (per-developer answers, not for the team).
# `_bmad/config.user.toml` is regenerated by `npx bmad-method install` and
# `_bmad/custom/config.user.toml` is the long-lived override surface.
_bmad/config.user.toml
_bmad/custom/config.user.toml
